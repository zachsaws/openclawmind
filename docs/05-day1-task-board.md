# Day 1 Task Board (稳上线计划)

## 今日目标

- 冻结玩法与交互的核心契约，避免后续前后端返工。
- 给前端提供稳定的 `API + WebSocket` 事件规范。
- 在代码中固化契约常量，启动“规范驱动开发”。

## 任务清单

1. 规则冻结（完成）
- 2副牌（含大小王），4人均分27张。
- AI（seat=1）先手。
- 回合阶段：`turn_open` / `challenge_window`。

2. API/事件契约冻结（完成）
- 冻结核心 API：`/games`、`/start`、`/actions/*`、`/rematch`、`/state`。
- 冻结 WS 事件：`game:subscribe`、`game:update`。

3. 前端交互基线（完成）
- 底部手牌区 + 提示/出牌/质疑/不质疑。
- 牌桌中心可见牌堆（背牌）与倒计时。

4. 服务端基线能力（完成）
- 安全头、压缩、限流、健康检查。
- 本地持久化战报落盘。

5. 自动化检查（完成）
- `npm run build`。
- `npm test`。

## 明日切入（Day 2）

1. 接入数据库（PostgreSQL）与会话缓存（Redis）。
2. 提供登录态（游客 + token）与房间权限校验。
3. 增加 E2E 流程测试（开局到结算）。
